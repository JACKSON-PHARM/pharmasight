# =============================================================================
# PharmaSight – Environment variables for Render
# =============================================================================
# Copy these variable NAMES into Render Dashboard → Service → Environment.
# Paste the VALUES from your local .env (do not commit .env or real secrets).
#
# In Render: Environment → Add Environment Variable → Key = name below, Value = from your .env
# =============================================================================

# -----------------------------------------------------------------------------
# Required for app + database
# -----------------------------------------------------------------------------
DEBUG=False
DATABASE_URL=
SECRET_KEY=

# -----------------------------------------------------------------------------
# Supabase (required if you use Supabase for DB and/or Auth)
# -----------------------------------------------------------------------------
SUPABASE_URL=
SUPABASE_KEY=
SUPABASE_SERVICE_ROLE_KEY=
SUPABASE_DB_PASSWORD=
SUPABASE_DB_HOST=
SUPABASE_DB_NAME=postgres
SUPABASE_DB_PORT=5432
SUPABASE_DB_USER=postgres

# Optional: if set, this email cannot be used as tenant admin (avoids Supabase Auth conflicts)
# SUPABASE_OWNER_EMAIL=

# -----------------------------------------------------------------------------
# Master DB (optional – for tenant registry; default = same as DATABASE_URL)
# -----------------------------------------------------------------------------
# MASTER_DATABASE_URL=

# -----------------------------------------------------------------------------
# Admin panel (required for tenant management login)
# -----------------------------------------------------------------------------
ADMIN_PASSWORD=

# Optional: admin email for display; username is always "admin"
# ADMIN_EMAIL=admin@pharmasight.com

# -----------------------------------------------------------------------------
# CORS (Render default: use * or your frontend URL)
# -----------------------------------------------------------------------------
CORS_ORIGINS=*

# -----------------------------------------------------------------------------
# Invite links (optional – if unset, links use your Render URL automatically)
# -----------------------------------------------------------------------------
# APP_PUBLIC_URL=https://pharmasight.onrender.com

# -----------------------------------------------------------------------------
# Email – invite emails (optional; if unset, invite link is still shown in UI to copy)
# -----------------------------------------------------------------------------
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
EMAIL_FROM=PharmaSight <noreply@pharmasight.com>

# -----------------------------------------------------------------------------
# Optional – Stripe (only if you use Stripe subscriptions)
# -----------------------------------------------------------------------------
# STRIPE_SECRET_KEY=
# STRIPE_WEBHOOK_SECRET=

# -----------------------------------------------------------------------------
# Optional – Supabase project provisioning (only if you auto-create Supabase projects)
# -----------------------------------------------------------------------------
# SUPABASE_ACCESS_TOKEN=
# SUPABASE_ORGANIZATION_ID=
# SUPABASE_REGION=us-east-1
# SUPABASE_PLAN=free
